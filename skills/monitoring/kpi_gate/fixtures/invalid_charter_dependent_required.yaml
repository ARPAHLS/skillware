schema_version: 2
policy_id: dependent_required_violation_demo
metrics: [replies, reply_to_booking_rate_pct]
rules:
  - id: BOOKING_CONVERSION_TRACKED
    metric: reply_to_booking_rate_pct
    check: {op: gte, threshold: 10}
    severity: warning
    requires: {min_denominator: 25, reason_code: cohort_attribution_unavailable}
