name: "New Skill Proposal"
description: Propose a new capability for the Skillware registry (not an upgrade to an existing skill).
title: "[New Skill]: "
labels: ["skill request", "enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for helping expand the Skillware ecosystem!
        Please review the [Contribution Guidelines](https://github.com/ARPAHLS/skillware/blob/main/CONTRIBUTING.md) and start from [templates/python_skill/](https://github.com/ARPAHLS/skillware/tree/main/templates/python_skill).

        See the [Skill categories](https://github.com/ARPAHLS/skillware/blob/main/CONTRIBUTING.md#skill-categories) table for the list of valid categories used in the dropdown below. Maintainers may apply the matching `cat: <category>` label when triaging (see [label taxonomy](https://github.com/ARPAHLS/skillware/blob/main/CONTRIBUTING.md#getting-started)).

        **Issuer org:** Optional `issuer.org` — `ARPAHLS` for ARPA-driven registry skills, contributor org for third-party skills, comma-separated values for co-affiliation (e.g. `ARPAHLS, AO`), or omit. See [Issuer org](https://github.com/ARPAHLS/skillware/blob/main/CONTRIBUTING.md#issuer-org).

        To improve an **existing** skill, use the **Skill Upgrade** template instead.

  - type: input
    id: skill_name
    attributes:
      label: Skill ID
      description: Full registry ID in `category/skill_name` form
      placeholder: finance/stock_checker
    validations:
      required: true

  - type: dropdown
    id: category
    attributes:
      label: Category
      description: Pick an existing category or propose a new one in the description
      options:
        - compliance
        - creative
        - data_engineering
        - defi
        - dev_tools
        - finance
        - monitoring
        - office
        - optimization
        - security
        - wellness
        - "Propose new category (describe below)"
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: What should this skill do?
      description: Describe the capability. What problem does it solve?
    validations:
      required: true

  - type: textarea
    id: inputs_outputs
    attributes:
      label: Ideal Inputs & Outputs
      description: |
        What arguments should it take? What JSON should it return?
      placeholder: |
        Input: {"ticker": "AAPL"}
        Output: {"price": 150.00, "change": "+1.2%"}
    validations:
      required: true

  - type: dropdown
    id: runtime
    attributes:
      label: Target runtime
      description: Which LLM runtimes must the skill support?
      options:
        - Model agnostic (all supported adapters)
        - Gemini
        - Claude
        - OpenAI
        - DeepSeek
        - Ollama (prompt mode)
        - Other / mixed
    validations:
      required: true

  - type: textarea
    id: env_vars
    attributes:
      label: External APIs & env vars (if any)
      description: List API keys or data sources the skill needs
      placeholder: |
        ETHERSCAN_API_KEY — optional chain enrichment
    validations:
      required: false
